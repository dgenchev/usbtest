The default project dir is /data/rprojects. It stores the R project itself.
The dafault data dir is /data/usbgeo. It stores the data for the project.
/data has 500GB of space and will be used for storage.